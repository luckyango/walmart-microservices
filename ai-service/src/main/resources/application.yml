server:
  port: 8085

spring:
  application:
    name: ai-service

springdoc:
  swagger-ui:
    path: /swagger-ui.html

services:
  gateway-base-url: http://localhost:8080

openai:
  api-key: ${OPENAI_API_KEY:}
  model: gpt-4.1-mini
